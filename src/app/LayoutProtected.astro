<script>
  import { SecureSessionManager } from '../auth/secureMiddleware';
  
  const sessionManager = new SecureSessionManager();
  if (!(await sessionManager.isAuthenticated())) {
    throw new Error('Not authenticated');
  }
  </script>
  
  <slot />